import { css } from 'lit';

export const yangChunCommentStyles = css`
  :host {
    display: block;
    --ycc-color: #de0c0cff;
    --ycc-padding: 12px;
  }

  .root {
    padding: var(--yyc-padding);
    color: var(--yyc-color);
  }
`;
