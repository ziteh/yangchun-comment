colors: true
no_tty: false

pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: npx lint-staged

commit-msg:
  commands:
    'lint commit message':
      run: pnpm exec commitlint --edit {1}
